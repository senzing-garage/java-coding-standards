{
  "_comment": [
    "Keys to merge into the consumer project's .vscode/settings.json.",
    "",
    "format_file.py is the single end-user entry point — a thin",
    "wrapper that invokes the tree-sitter-based AST formatter at",
    "format_java.py in-process. Same input → same output, regardless",
    "of caller. format-on-save wires emeraldwalk.runonsave directly",
    "to format_file.py so every save runs the canonical formatter.",
    "",
    "We disable the redhat.java extension's built-in formatOnSave",
    "(`[java].editor.formatOnSave: false`) because the redhat.java",
    "formatter doesn't implement the Senzing spec — running it on",
    "save would produce output that format_file.py then has to",
    "rewrite anyway. emeraldwalk.runonsave invokes the canonical",
    "formatter once on save instead.",
    "",
    "If the user declines format-on-save during adoption, omit the",
    "emeraldwalk.runonsave block; the [java] override stays",
    "regardless (the user can still trigger the formatter via the",
    "keybinding from tasks.json)."
  ],
  "[java]": {
    "editor.formatOnSave": false
  },
  "emeraldwalk.runonsave": {
    "commands": [
      {
        "match": "\\.java$",
        "cmd": "python3 \"${workspaceFolder}/.java-coding-standards/tooling/scripts/format_file.py\" \"${file}\""
      }
    ]
  }
}
