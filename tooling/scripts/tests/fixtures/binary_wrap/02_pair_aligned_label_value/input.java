public class Demo
{
    private String name;
    private int age;
    private String status;

    public String toString()
    {
        return "name=[ " + name + " ], age=[ " + age + " ], status=[ " + status + " ]";
    }
}
