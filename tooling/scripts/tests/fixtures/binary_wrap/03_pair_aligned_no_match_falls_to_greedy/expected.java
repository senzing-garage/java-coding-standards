public class Demo
{
    public String build(String prefix,
                        String first,
                        String second,
                        String third,
                        String fourth)
    {
        return prefix + first + "/" + second + "/" + third + "/" + fourth
            + "/end-marker";
    }
}
