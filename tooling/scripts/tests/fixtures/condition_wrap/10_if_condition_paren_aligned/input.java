public class Demo
{
    public void run(int firstValue, int secondValue, int thirdValue)
    {
        if (firstValue > 0 && secondValue > 0 && thirdValue > 0 && firstValue < 100)
        {
            return;
        }
    }
}
