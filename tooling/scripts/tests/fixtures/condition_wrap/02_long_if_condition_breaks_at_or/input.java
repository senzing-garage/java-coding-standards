public class Demo
{
    public void run(boolean firstCondition, boolean secondCondition, boolean thirdConditionWithLongerName)
    {
        if (firstCondition || secondCondition || thirdConditionWithLongerName) {
            return;
        }
    }
}
