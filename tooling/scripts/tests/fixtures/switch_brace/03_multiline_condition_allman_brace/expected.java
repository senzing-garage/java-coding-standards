public class Demo
{
    void run()
    {
        switch (alphaVariableName + betaVariableName + gammaVariableName
                + deltaName)
        {
            case ALPHA:
                doAlpha();
                break;
        }
    }
}
