public class Demo
{
    public String pick(boolean condition)
    {
        return (condition
                ? "this is the consequence value to return"
                : "this is the alternative value to return");
    }
}
