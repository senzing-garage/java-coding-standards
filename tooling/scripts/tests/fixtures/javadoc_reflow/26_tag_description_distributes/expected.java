public class Demo
{
    /**
     * Filters the sample scenario.
     *
     * @param principle The principle to filter on which can be
     *                  <code>null</code> to indicate only the counts
     *                  not associated with a specific principle
     *                  should be included, or <code>"*"</code> to
     *                  indicate no filtering, or a specific principle.
     */
    public void filter(String principle)
    {
    }
}
