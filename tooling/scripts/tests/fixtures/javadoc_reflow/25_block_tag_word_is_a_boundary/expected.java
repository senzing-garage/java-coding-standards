public class Demo
{
    /**
     * Returns the elapsed time in milliseconds for this batch, measured from
     * the moment it was created until the moment it was closed. Overrides of
     * this method must be annotated @Override so the compiler can verify the
     * contract.
     */
    public long getElapsed()
    {
        return 0L;
    }
}
