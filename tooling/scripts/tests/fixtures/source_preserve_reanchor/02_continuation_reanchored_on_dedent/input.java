public class Demo
{
    void run()
    {
        if (flag) {
            if (other) {
                    assertEquals(customersConfig, configJson,
                                 "Unexpected configuration definition.");
            }
        }
    }
}
