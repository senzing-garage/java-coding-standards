# Test-only dependencies for tooling/scripts/tests/.
#
# Install locally with:
#     pip install -r tooling/scripts/tests/requirements.txt
# Run with:
#     pytest tooling/scripts/tests/
#
# CI installs this same file in the pytest workflow. Pulls in the
# runtime dependencies so the formatter under test has its
# tree-sitter / tree-sitter-java imports available.

-r ../requirements.txt

pytest>=9.1.0
